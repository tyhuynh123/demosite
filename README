This Ansible playbook deploy a upload form of your pic into a photo gallery. 

To Deploy, follow the instructions below. 
 
Before you deploy, Please have a ssh rsa plubic key created, if you do not have a ssh rsa public key. 

Run this command < ssh-keygen -t rsa > to create one. 

Please run "prerequisites.sh" script and make it executable by < chmod +x prerequisites.sh >   

Before you can run the Ansible playbook please run <aws configure> command and add your AWS Access key plus Secret.

Run your Ansible Playbook <ansible-playbook -i host deploy_mydemosite.yml>



